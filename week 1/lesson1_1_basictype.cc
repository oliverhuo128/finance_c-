#include <string>

int main() { 
         // integer variable. 
         // It can only hold integers
         int a = 42;
         
         // float variable. 
         // It can only hold floats -1000.01… etc.
         float b = 3.141;   
         
         // string variable. 
         // It can only hold strings. Note ""
         std::string c = "Hello World";   
         
         // change data in 'a' to be another value ,
         // don't specify the type again
         a = 15;
         return 0;
} 
