// math example
#include <iostream>     // std::cout
#include <cmath>         // math function

int main () { 
  double p = 1.2;
  std::cout << "sin(p) " << std::sin(p) << '\n'; //in radians
  std::cout << "cos(p) " << std::cos(p) << '\n';
  return 0;
}